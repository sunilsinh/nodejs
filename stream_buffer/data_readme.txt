   buffer-

-Temprary storage spot for a chunk of data that is being transferred from one place to
another.

-The buffer is fillded with data, then passed along.

-Transfer small chunks of data at a time.


Streams in node js

- Can create streams in node.js to transfer data
- Increase performance

- Writable streams- allow node js to write data to stream
- Readable stream- allow node js to read data from a stream
- Duplex- can read and write a stream
